# Optional: point at a Turso hosted libSQL.
# Leave empty (default) to use a local file at <userData>/.superhive/data.db
# Example: libsql://your-db.turso.io
LIBSQL_URL=
# Optional: auth token for the hosted libSQL DB above.
LIBSQL_AUTH_TOKEN=
